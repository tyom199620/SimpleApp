import React from 'react';
import { View } from 'react-native';

import scale from '../../helpers/Theme';

export default function Footer(params) {

    return <View style={{ height: 19 / scale }} />
}